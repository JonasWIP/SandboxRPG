// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void PickupItemHandler(ReducerEventContext ctx, ulong worldItemId);
        public event PickupItemHandler? OnPickupItem;

        public void PickupItem(ulong worldItemId)
        {
            conn.InternalCallReducer(new Reducer.PickupItem(worldItemId));
        }

        public bool InvokePickupItem(ReducerEventContext ctx, Reducer.PickupItem args)
        {
            if (OnPickupItem == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnPickupItem(
                ctx,
                args.WorldItemId
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class PickupItem : Reducer, IReducerArgs
        {
            [DataMember(Name = "world_item_id")]
            public ulong WorldItemId;

            public PickupItem(ulong WorldItemId)
            {
                this.WorldItemId = WorldItemId;
            }

            public PickupItem()
            {
            }

            string IReducerArgs.ReducerName => "pickup_item";
        }
    }
}
